let form = document.querySelector('form');



form.addEventListener('submit', function (event, userEmail, userPassword) {

    event.preventDefault();
    userEmail = document.getElementById('user-email').value;
    userPassword = document.getElementById('user-password').value;
    switch (userEmail) {
        case userEmail.include("@admdente.com.br"):
            switch(userEmail){
                case userEmail.include("gabrielm"):
            }
        case userEmail.include("@doctordente.com.br"):
            switch(userEmail){
            
            }

        case userEmail.include("@secretarydente.com.br"):

        default:
            if(userEmail === "patient@gmail.com")
            break;

    }

})

